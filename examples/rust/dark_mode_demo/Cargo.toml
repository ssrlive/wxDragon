[package]
name = "dark_mode_demo"
version = "0.1.0"
edition = "2021"

[workspace]
# Empty workspace table to prevent this package from being part of the parent workspace

[dependencies]
wxdragon = { path = "../../../rust/wxdragon", features = ["xrc"] }
