[package]
name = "gallery"
version = "0.1.0"
edition = "2021"

[dependencies]
# Depend on the safe wrapper crate now
wxdragon = { path = "../../../rust/wxdragon", features = ["aui"] }
image = { version = "0.25.1", default-features = false, features = ["png", "gif"] }

[build-dependencies]
embed-manifest = "1.4"